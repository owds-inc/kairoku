{
  "name": "kairoku",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "kairoku": "./src/cli/main.ts"
  },
  "scripts": {
    "start": "bun run src/daemon/server.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build:release": "bun run scripts/build-release.ts",
    "cut": "bun run scripts/cut.ts",
    "prune": "bun run src/daemon/prune.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.259"
  }
}
